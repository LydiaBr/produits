{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Editer{% endblock %}

{% block body %}
    <div>Modifier un produit</div>
    {{ form_start(produitForm) }}
    {{ form_widget(produitForm) }}
    <button>Modifier</button>
    {{ form_end(produitForm) }}

{% endblock %}